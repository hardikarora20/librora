/**
 * DTOs for the auth module (e.g. RegisterRequest, LoginRequest, AuthResponse).
 * Entities are never exposed directly to the client — only DTOs cross the API boundary.
 */
package com.bookbrief.auth.dto;
