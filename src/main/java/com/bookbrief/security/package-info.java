/**
 * Security module.
 * Responsibilities: Spring Security configuration, JWT filter/provider, password encoding.
 * Contains: SecurityConfig, JwtAuthFilter, JwtUtil (added in Phase 7).
 */
package com.bookbrief.security;
